import MainContainer from "../../layout/MainContainer";
import React from "react";

const Authentication = (props) => {

    return (
        <MainContainer
            section="login"
            title={<h1>Welcome to Pencilmark</h1>}>
            <div>
            </div>
        </MainContainer>
    )
}

export default (Authentication);